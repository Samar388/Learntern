server {
  listen 80;
  server_name _;

  # Frontend requests
  location / {
    proxy_pass http://frontend:3000;  # Frontend service
  }

  # Backend API requests
  location /api {
    proxy_pass http://app;  # Backend service
    proxy_set_header Host $host;
  }
}
